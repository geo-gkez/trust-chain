# Ανάπτυξη Συστήματος Ιχνηλάτησης Προϊόντων Εφοδιαστικής Αλυσίδας με Τεχνολογία Blockchain

## Αντικείμενο της Εργασίας

Οι φοιτητές καλούνται να σχεδιάσουν και να υλοποιήσουν ένα σύστημα ιχνηλάτησης προϊόντων στην εφοδιαστική αλυσίδα (π.χ. τρόφιμα, φαρμακευτικά προϊόντα, βιομηχανικά αγαθά) από το στάδιο της παραγωγής έως τη διάθεση στον τελικό καταναλωτή. Το σύστημα θα βασίζεται σε τεχνολογία blockchain και Έξυπνα Συμβόλαια - ΕΣ (Smart Contracts) και θα έχει ως στόχο τη διαφάνεια, την αποτροπή απάτης και τη βελτίωση της αξιοπιστίας των διαδικασιών διακίνησης προϊόντων. Μέσω των ΕΣ και της ανάπτυξης των κατάλληλων συναρτήσεων θα πρέπει να εξασφαλίζεται η αυθεντικότητα και η ακεραιότητα των δεδομένων που αποθηκεύονται στο blockchain.

## Γενική Λειτουργία της εφαρμογής

Κατά την έναρξη λειτουργίας της εφαρμογής, θα πρέπει να έχει ήδη οριστεί τουλάχιστον ένας λογαριασμός διαχειριστή, ο οποίος θα μπορεί να εγγράφει χρήστες στο σύστημα αποδίδοντάς τους συγκεκριμένους ρόλους. Οι εγγεγραμμένοι χρήστες έχουν τη δυνατότητα να καταχωρούν προϊόντα, εισάγοντας βασικές πληροφορίες, όπως τον τύπο προϊόντος, την κατάσταση και την τοποθεσία. Στη συνέχεια, το κάθε προϊόν παρακολουθείται καθ' όλη τη διάρκεια της διαδρομής του – από την παραγωγή, την αποθήκευση και τη μεταφορά, έως την τελική του διάθεση στον καταναλωτή. Παράλληλα, η εφαρμογή παρέχει έναν ρόλο επόπτη (Ελεγκτής / Ρυθμιστική Αρχή), με δυνατότητα πλήρους πρόσβασης στα δεδομένα για λόγους διαφάνειας και εποπτείας. Μέσω μιας διεπαφής χρήστη (UI), επιτρέπεται η εγγραφή και η είσοδος χρηστών με διαφορετικά δικαιώματα, η προβολή της τρέχουσας κατάστασης κάθε προϊόντος (ανάλογα με τα δικαιώματα του χρήστη), καθώς και η αναζήτηση πληροφοριών βάσει μοναδικού αναγνωριστικού.

## Προδιαγραφές εφαρμογής

Οι φοιτητές καλούνται να:

### 1. Σχεδιάσουν και υλοποιήσουν ένα (ή περισσότερα) Έξυπνο/α Συμβόλαιο/α σε γλώσσα **Solidity** (ή άλλη κατάλληλη) για την καταγραφή:

- της δημιουργίας μιας παρτίδας προϊόντος (με πληροφορίες για παραγωγό, τοποθεσία, ημερομηνία),
- της μεταφοράς και αποθήκευσης σε διαδοχικά στάδια (με ημερομηνίες, τοποθεσίες και παρόχους υπηρεσιών),
- της τελικής διάθεσης του προϊόντος στον καταναλωτή ή σε σημείο πώλησης.

### 2. Ορίσουν τουλάχιστον **έξι τύπους χρηστών** με ρόλους και περιορισμένα δικαιώματα:

- **Διαχειριστής:** Διαχειρίζεται την εγγραφή χρηστών στο σύστημα.
- **Παραγωγός:** Δημιουργεί παρτίδες και εισάγει βασικά στοιχεία.
- **Μεταφορέας:** Καταχωρεί δεδομένα για τη μεταφορά προϊόντων.
- **Αποθήκη / Κέντρο Διανομής:** Καταχωρεί δεδομένα αποθήκευσης και διαχείρισης προϊόντων.
- **Διανομέας / Λιανοπωλητής:** Καταχωρεί την παραλαβή και διάθεση προϊόντων.
- **Ελεγκτής / Ρυθμιστική Αρχή:** Έχει πλήρη πρόσβαση για έλεγχο δεδομένων και εποπτεία.

### 3. Αναπτύξουν μια **διεπαφή χρήστη (UI)** που θα επιτρέπει:

- Εγγραφή και σύνδεση χρηστών με διαφορετικούς ρόλους.
- Καταχώρηση νέων παρτίδων προϊόντων.
- Παρακολούθηση της διαδρομής των προϊόντων μέσω του blockchain.
- Αναζήτηση πληροφοριών βάσει μοναδικού αναγνωριστικού της παρτίδας.
- Εμφάνιση όλων των στοιχείων ανάλογα με τον ρόλο του χρήστη.

### 4. Καταχωρίσουν τουλάχιστον **10 παρτίδες προϊόντων** με:

- Μοναδικό κωδικό (serial/ID)
- Τύπο προϊόντος (τρόφιμο, φάρμακο, εξάρτημα κ.λπ.)
- Κατηγορία (ευπαθές, μη ευπαθές κ.λπ.)
- Κατάσταση (παραγωγή, αποθήκευση, μεταφορά, διάθεση)

### 5. Καταγράψουν τουλάχιστον δύο πλήρεις διαδρομές παρτίδων από παραγωγή έως πώληση για να παρουσιαστεί το σύστημα end-to-end.

### 6. Ενσωματώσουν έναν βασικό μηχανισμό ασφάλειας στο σύστημα, όπως:

- Καταγραφή (logging) όλων των κρίσιμων ενεργειών μέσω events στο blockchain, και
- Έλεγχο εγκυρότητας χρήστη πριν από κρίσιμες ενέργειες (π.χ. ενημέρωση κατάστασης προϊόντος), ώστε να αποτρέπονται μη εξουσιοδοτημένες ενέργειες.

### 7. Να διενεργήσουν **βασικό έλεγχο ασφάλειας** (security audit) του/των Έξυπνου/ων Συμβολαίου/ων που ανέπτυξαν, χρησιμοποιώντας τουλάχιστον ένα εργαλείο ανοιχτού κώδικα (open-source), όπως τα **Solhint**, **Slither** ή **Mythril**. Ο έλεγχος θα πρέπει να περιλαμβάνει εντοπισμό πιθανών ευπαθειών (π.χ. reentrancy, integer overflows, access control issues), καθώς και σύντομη αναφορά των ευρημάτων και τυχόν διορθωτικών ενεργειών.

---

## Χρήσιμες Οδηγίες

> **Προσοχή! Οι οδηγίες δεν αποτελούν αποκλειστικά βήματα της υλοποίησης**

- Καθορίστε έναν λογαριασμό Διαχειριστή. Ο λογαριασμός αυτός μπορεί να είναι ο αποστολέας (`msg.sender`) του contract κατά την ανάπτυξή του στο δίκτυο. Αυτός θα έχει δικαίωμα εγγραφής νέων χρηστών και ορισμού ρόλων.

- Καθορίστε μια δομή δεδομένων (`struct`) για τις παρτίδες προϊόντων με τις κατάλληλες μεταβλητές. Για παράδειγμα:

  ```
  id:           μοναδικό αναγνωριστικό παρτίδας (π.χ. hash ή α/α)
  productType:  τύπος προϊόντος
  category:     κατηγορία (παραγωγή, μεταφορά, αποθήκευση, διάθεση)
  origin:       περιοχή προέλευσης
  creationDate: ημερομηνία δημιουργίας / ημερομηνία συγκομιδής
  status:       στάδιο (παραγωγή, μεταφορά, αποθήκευση, διάθεση)
  currentHolder: διεύθυνση χρήστη που έχει την παρτίδα
  ```

- Καθορίστε μια δομή δεδομένων (`struct`) για τους χρήστες στην οποία να περιλαμβάνεται και μια μεταβλητή που θα καθορίζει τον ρόλο του χρήστη. Αυτή η μεταβλητή μπορεί να χρησιμοποιηθεί κατά τον έλεγχο της πρόσβασης των χρηστών σε δεδομένα.

  ```
  UserAddress: Ethereum address
  name:        όνομα χρήστη
  role:        ρόλος (παραγωγός, μεταφορέας, επεξεργαστής κ.λπ.)
  active:      ενεργός/ανενεργός χρήστης
  ```

- Συσχετίστε τους χρήστες με τις παρτίδες προϊόντων.

- Δημιουργήστε συναρτήσεις στο ΕΣ. Κατασκευάστε συναρτήσεις για: Καταχώρηση νέας παρτίδας (μόνο από παραγωγό), Ενημέρωση κατάστασης παρτίδας (μόνο από εξουσιοδοτημένο ρόλο), Εγγραφή χρηστών με καθορισμό ρόλου (μόνο από διαχειριστή), Πιστοποίηση παρτίδας από ελεγκτή (με ενημέρωση πεδίου `certified`), Προβολή όλων των δεδομένων παρτίδας με `view` συνάρτηση.

- Βεβαιωθείτε ότι οι συναρτήσεις είναι ασφαλείς και αποτρέπουν τις μη εξουσιοδοτημένες τροποποιήσεις των δεδομένων. Δημιουργήστε `modifiers`, όπως `onlyAdmin` για συναρτήσεις που αφορούν τη διαχείριση χρηστών, αντίστοιχα `onlyProducer`, `onlyTransporter`, `onlyProcessor`.

---

## Παραδοτέα

Τα παραδοτέα της απαλλακτικής εργασίας περιλαμβάνουν:

- Ένα **αρχείο PDF** που θα περιλαμβάνει τα εξής:
  - Περιγραφή του προβλήματος και της σημασίας της ιχνηλασιμότητας αγροτικών προϊόντων.
  - Αναλυτική παρουσίαση της αρχιτεκτονικής του συστήματος.
  - Ανάλυση των ρόλων, των δικαιωμάτων και της ροής λειτουργίας.
  - Περιγραφή των έξυπνων συμβολαίων και των συναρτήσεων που περιλαμβάνουν.
  - Περιγραφή της διεπαφής χρήστη με στιγμιότυπα (screenshots) και περιγραφή λειτουργιών.
  - Συμπεράσματα και προτάσεις για επεκτασιμότητα ή εφαρμογή στην πράξη.

- Ο **κώδικας** του/των έξυπνου/ων συμβολαίου/ων σε ξεχωριστό/α αρχείο/α **.sol** με σχολιασμό.

- Πηγαίος κώδικας της διεπαφής χρήστη (UI).

- Σύντομες Οδηγίες εγκατάστασης και χρήσης του συστήματος.

---

## Σημείωση

1. Για την ανάπτυξη και δοκιμή του Έξυπνου Συμβολαίου μπορεί να χρησιμοποιηθεί το διαδικτυακό εργαλείο remix IDE: <https://remix.ethereum.org/>

2. Για την ανάπτυξη επίσης μπορεί να χρησιμοποιηθεί:
   - (i) η σουίτα εργαλείων Truffle (<https://archive.trufflesuite.com/>) και τα Truffle boxes (<https://archive.trufflesuite.com/boxes/>)
   - (ii) το Hardhat (<https://hardhat.org/>)
   - (iii) Visual Studio κλπ

3. Για τα εργαλεία εύρεση ευπαθειών ανοιχτού κώδικα μπορούν να χρησιμοποιηθούν:
   - <https://protofire.io/solhint>
   - <https://github.com/crytic/slither>
   - <https://github.com/ConsenSysDiligence/mythril>

4. Στην βαθμολόγηση των εργασιών αξιολογείται και η βέλτιστη λύση που θα αναπτυχθεί για την εφαρμογή.

---

**Η εργασία μπορεί να εκπονηθεί από ομάδες των 2-3 ατόμων.**
